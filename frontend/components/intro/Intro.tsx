import React, { ReactElement } from 'react';
import MainTitle from './MainTitle';

const Intro = () => {
    return (
        <div>
            <MainTitle />
        </div>
    );
};

export default Intro;
